name: Validate JSON

on:
  pull_request:
    paths:
      - 'registry.json'
      - 'dashboards/**/*.json'
      - 'presets/**/*.json'
      - 'card-presets/**/*.json'
      - 'themes/**/*.json'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate registry.json
        run: |
          echo "Validating registry.json..."
          python3 -m json.tool registry.json > /dev/null
          echo "registry.json is valid JSON"

      - name: Validate dashboard files
        run: |
          errors=0
          for f in dashboards/*/dashboard.json; do
            if [ -f "$f" ]; then
              if python3 -m json.tool "$f" > /dev/null 2>&1; then
                echo "OK: $f"
              else
                echo "INVALID: $f"
                errors=$((errors + 1))
              fi
            fi
          done
          if [ $errors -gt 0 ]; then
            echo "Found $errors invalid JSON file(s)"
            exit 1
          fi

      - name: Validate dashboard format
        run: |
          python3 - <<'SCRIPT'
          import json, sys, glob, os

          errors = 0
          for path in glob.glob("dashboards/*/dashboard.json"):
              with open(path) as f:
                  data = json.load(f)

              if data.get("format") != "kc-dashboard-v1":
                  print(f"ERROR: {path} missing or wrong 'format' field (expected 'kc-dashboard-v1')")
                  errors += 1

              if not data.get("name"):
                  print(f"ERROR: {path} missing 'name' field")
                  errors += 1

              if not isinstance(data.get("cards"), list):
                  print(f"ERROR: {path} missing or invalid 'cards' array")
                  errors += 1
              else:
                  for i, card in enumerate(data["cards"]):
                      if not card.get("card_type"):
                          print(f"ERROR: {path} cards[{i}] missing 'card_type'")
                          errors += 1
                      if not isinstance(card.get("position"), dict):
                          print(f"ERROR: {path} cards[{i}] missing 'position'")
                          errors += 1

              print(f"OK: {path} ({len(data.get('cards', []))} cards)")

          # Validate registry entries match actual files
          with open("registry.json") as f:
              registry = json.load(f)

          all_entries = registry.get("items", []) + registry.get("presets", [])
          for item in all_entries:
              item_type = item.get("type", "")
              item_id = item.get("id", "?")

              if item_type == "dashboard":
                  expected = f"dashboards/{item_id}/dashboard.json"
              elif item_type == "card-preset":
                  if os.path.isfile(f"presets/{item_id}.json"):
                      expected = f"presets/{item_id}.json"
                  else:
                      expected = f"card-presets/{item_id}.json"
              elif item_type == "theme":
                  expected = f"themes/{item_id}.json"
              else:
                  print(f"WARN: registry entry '{item_id}' has unknown type '{item_type}'")
                  continue

              if os.path.isfile(expected):
                  print(f"OK: registry entry '{item_id}' ({item_type}) has matching file")
              else:
                  print(f"ERROR: registry entry '{item_id}' ({item_type}) missing file at {expected}")
                  errors += 1

          if errors > 0:
              print(f"\n{errors} error(s) found")
              sys.exit(1)

          print("\nAll validations passed")
          SCRIPT
