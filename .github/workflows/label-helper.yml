name: Label Helper

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-helper:
    # pinned to kubestellar/infra main @ 2026-06-19 (supply-chain fix #374)
    uses: kubestellar/infra/.github/workflows/reusable-label-helper.yml@1a04a3fd17771e78e5cf6d42a3fda84e3ba478f3
    # secrets: inherit removed — reusable-label-helper.yml declares no secret inputs
