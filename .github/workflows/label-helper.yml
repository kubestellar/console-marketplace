name: Label Helper

on:
  issue_comment:
    types: [created]

permissions: read-all  # default read; writes scoped to job below

jobs:
  label-helper:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: kubestellar/infra/.github/workflows/reusable-label-helper.yml@a160acca0bdce1ac6c649e006d680d5f6d53024e  # main
    secrets: inherit
