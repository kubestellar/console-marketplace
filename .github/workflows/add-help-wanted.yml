name: Add Help Wanted Label

on:
  issues:
    types: [labeled]

permissions: read-all  # default read; writes scoped to job below

jobs:
  label:
    permissions:
      issues: write
    uses: kubestellar/infra/.github/workflows/reusable-add-help-wanted.yml@a160acca0bdce1ac6c649e006d680d5f6d53024e  # main
    secrets: inherit
