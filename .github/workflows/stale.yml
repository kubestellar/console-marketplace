name: Stale Issues

on:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:

permissions:
  issues: write

jobs:
  stale:
    # pinned to kubestellar/infra main @ 2026-06-19 (supply-chain fix #374)
    uses: kubestellar/infra/.github/workflows/reusable-stale.yml@1a04a3fd17771e78e5cf6d42a3fda84e3ba478f3
    # secrets: inherit removed — reusable-stale.yml declares no secret inputs
